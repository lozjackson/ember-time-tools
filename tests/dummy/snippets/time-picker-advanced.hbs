{{time-picker
  selectedTime=selection
  output="date|timestamp|object"
  displayFormat='hh:mm a'
  timeInterval=30
  scrollToSelectedTime=true|false
  select=(action "selectTime")
  close=(action "closeTimePicker")}}
