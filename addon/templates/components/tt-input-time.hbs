{{#if hasBlock}}
  {{yield}}
{{else}}
  {{tt-time-field value=(format-date value "time") toggleTimePicker="toggleTimePicker"}}
  {{#if showTimePicker}}
    {{tt-time-picker selectedTime=value select="selectTime" close="closeTimePicker"}}
  {{/if}}
{{/if}}
